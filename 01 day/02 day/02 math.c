#include<stdio.h>


main(){
       
       float length,area;
       printf("Enter the length of the rectagle:");
       scanf("%f",&length);
       area= length*length;
       printf("the area of circle%f",area);
}
       
       
